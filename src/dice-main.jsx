import React from "react"
import ReactDOM from "react-dom/client"
import Dice from "./page/CubeAnimation/index"
import "./maze-entry.css"

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <Dice />
    </React.StrictMode>
)
